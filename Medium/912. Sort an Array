class Solution {

void mergeSort (vector<int>& nums){
    for (int i = 0; i <)
}

public:
    vector<int> sortArray(vector<int>& nums) {
        sort (nums.begin(), nums.end());
        return nums;
    }
};
