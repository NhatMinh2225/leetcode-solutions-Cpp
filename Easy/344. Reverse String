class Solution {
public:
    void reverseString(vector<char>& s) {
        int n = s.size() - 1;
        int count = 0;
        for (int i = n; i > (n/2)  ; i--){
            swap(s[count++], s[i]);
        }
    }
};
